<form method="post" action="{{ path('app_grupo_amigos_delete', {'id': grupo_amigo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grupo_amigo.id) }}">
    <button class="btn">Delete</button>
</form>
