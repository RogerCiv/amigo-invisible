{% extends 'base.html.twig' %}

{% block title %}Invitacion index{% endblock %}

{% block body %}
    <h1>Invitacion index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for invitacion in invitacions %}
            <tr>
                <td>{{ invitacion.id }}</td>
                <td>
                    <a href="{{ path('app_invitacion_show', {'id': invitacion.id}) }}">show</a>
                    <a href="{{ path('app_invitacion_edit', {'id': invitacion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_invitacion_new') }}">Create new</a>
{% endblock %}
